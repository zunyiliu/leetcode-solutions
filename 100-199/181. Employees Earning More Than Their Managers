SELECT
    a.Name AS 'Employee'
FROM
    Employee AS a,
    Employee AS b
WHERE
    a.ManagerId = b.Id
        AND a.Salary > b.Salary
;

// as is optional
select a.name as "employee"
from employee a, employee b
where a.managerid = b.id
and a.salary>b.salary;
